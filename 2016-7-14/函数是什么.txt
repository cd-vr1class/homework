函数是什么？
函数其实就是包装了一段代码

用途是什么？
这段代码经常会用到的时候，为了方便起见，把它包装起来了。

return 是什么？
就是函数计算的结果，你可以把return的值直接代换到函数调用的地方

不用return会怎么样？
当你的需求是可以直观显示的东西 ，或者能从现有的变量中观察到函数操作的结果，那么不用return也无所谓。
不用return函数会有个默认返回值,undefined

函数的语法
function 函数名([参数,参数....])
{
    包装的代码
}

函数的调用
函数名([具体的参数,具体的参数....])
